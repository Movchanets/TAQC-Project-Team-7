# fixtures/

This directory contains **Playwright fixtures** — reusable setup and teardown logic that extends the base `test` object.

**Examples of what goes here in future iterations:**
- Authenticated browser contexts (logged-in user sessions)
- Pre-seeded database states
- Custom `test` extensions that inject Page Objects automatically

**Current status:** Placeholder — no fixtures implemented in Iteration 1.
